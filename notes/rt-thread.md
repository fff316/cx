# RT-Thread

定义：嵌入式实时多线程操作系统

RT-Thread NANO为极简版本，只需3kb Flash，1.2kb RAM

- RAM可读写，掉电丢失，用于运行时数据处理
- ROM通常只读，掉电不丢失，传统 ROM 已经很少单独使用，用于单片机启动程序
- Flash可读写，掉电不丢失，按扇区操作，用于存储程序和用户数据（Flash统一放电全变1，单独充电变为0）
- EEPROM比Flash成本更高，可单个字节充放电，所以可以修改单个字节而不用按扇区操作